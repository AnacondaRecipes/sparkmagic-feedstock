channels:
  sk_test: main

# the conda-build parameters to use
build_parameters:
  - "--suppress-variables"
  - "--skip-existing"
  - "--error-overlinking"
  - "--error-overdepending"
  - "-c"
  - "sk_test"